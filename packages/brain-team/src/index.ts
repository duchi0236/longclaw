// Team brain package: the orchestrator decision policy as a third pluggable
// brain. It delegates work to subtasks (run in parallel by the runtime) and
// synthesizes their results — same contract, same tools, different decisions.
export * from "./team-brain.js";
